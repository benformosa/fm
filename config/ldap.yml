## Authorizations
# Uncomment out the merging for each environment that you'd like to include.
# You can also just copy and paste the tree (do not include the "authorizations") to each
# environment if you need something different per enviornment.
authorizations: &AUTHORIZATIONS
  group_base: cn=users,dc=fm,dc=internal
  ## Requires config.ldap_check_group_membership in devise.rb be true
  # Can have multiple values, must match all to be authorized
  required_groups:
    # If only a group name is given, membership will be checked against "uniqueMember"
    - ["member", "cn=fleet-manager-users,cn=users,dc=fm,dc=internal"]
    # If an array is given, the first element will be the attribute to check against, the second the group name
    #- ["moreMembers", "cn=users,cn=groups,dc=test,dc=com"]
  ## Requires config.ldap_check_attributes in devise.rb to be true
  ## Can have multiple attributes and values, must match all to be authorized
  #require_attribute:
    #objectClass: inetOrgPerson
    #authorizationRole: postsAdmin

  admin_group: ["member", "cn=fleet-manager-admins,cn=groups,dc=fm,dc=internal"]
   
## Environment

development:
  host: fm.internal
  port: 389
  attribute: samaccountname
  base: cn=users,dc=fm,dc=internal
  admin_user: cn=fm_ldap,cn=users,dc=fm,dc=internal
  admin_password: fm_ldap
  ssl: false
  <<: *AUTHORIZATIONS

test:
  host: localhost
  port: 389
  attribute: cn
  base: ou=users,dc=fm,dc=test
  admin_user: cn=fm_ldap,ou=users,dc=fm,dc=test
  admin_password: fm_ldap
  ssl: false
  <<: *AUTHORIZATIONS

production:
  host: fm.internal
  port: 636
  attribute: samaccountname
  base: cn=users,dc=fm,dc=internal
  admin_user: cn=fm_ldap,cn=users,dc=fm,dc=internal
  admin_password: fm_ldap
  ssl: start_tls
  <<: *AUTHORIZATIONS
